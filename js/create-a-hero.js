// Crea una clase llamada User,
// que define un constructor,
// que recibe 1 parámetro, dni

// A continuación, añade los setters y getters
// para el nombre y el email

// Crea un objeto, dale un nombre y un email
// y muestralos por consola

// class User {
//   constructor(dni) {
//     this.dni = dni;
//     this.emails = "";
//     this.name = "";
//   }
//   set userName(newName) {
//     this.name(newName);
//   }
//   set email(newEmail) {
//     this.email.push(newEmail);
//   }
//   get userName() {
//     console.log(this.name);
//   }
//   get email() {
//     console.log(this.emails);
//   }
// }
// let persona = new User("6666666V");
// persona.name = "Francisco"
// persona.emails= "hello@gmail.com"
// console.log(persona.dni);
// console.log(persona.name);
// console.log(persona.emails);

// Class User
/* class User {
    // Function constructor with a parameter.
    constructor(dni) {
        this.userName = '';
        this.userEmail = '';
        this.userDni = dni;
    }
    // Setters && Getters
    set name(name) {
        this.userName = name;
    }
    set email(email) {
        this.userEmail = email;
    }
    set dni(dni) {
        this.userDni = dni;
    }
    get dni() {
        return this.userName;
    }
    get name() {
        return this.userName;
    }
    get email() {
        return this.userEmail;
    }
}

let testUser = new User('12345678A');
console.log(testUser);

testUser.dni = '98765432B';
testUser.name = 'Example';
testUser.email = 'example@example.com';

console.log(testUser);

// testUser.getName;
// testUser.getEmail;
console.log(testUser.dni);
console.log(testUser.name);
console.log(testUser.email); */
// testUser.userDni;
